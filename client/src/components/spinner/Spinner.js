import React from 'react';
import { SpinnerContainer, SpinnerOverlay } from './spinner.styles';

export default () => {
  return (
    <SpinnerOverlay>
      <SpinnerContainer />
    </SpinnerOverlay>
  );
};
